import { View, Text } from "react-native";
import React from "react";

import * as Progress from "expo-progress";

const ProgressiveImage = () => {
  return (
    <View>
      <Text>ProgressiveImage</Text>
    </View>
  );
};

export default ProgressiveImage;
